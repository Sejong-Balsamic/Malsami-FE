## ✅ 작업 완료 — 질문상세 헤더 수정

### 🐞 원인
1. **헤더 고정 버그**: `detail/[id]/page.tsx`에서 `CommonHeader title="내 전공 질문"`이 하드코딩되어, 어떤 질문을 열어도 항상 "내 전공 질문"으로 표시됐습니다.
2. **PC 흰 배경 넘침**: 최상위 컨테이너가 `bg-white`라 PC에서 `max-w-[640px]` 컨테이너 바깥(회색이어야 할 영역)까지 전부 흰색으로 칠해졌습니다.

### 🛠 수정 내용
1. 헤더 `title`을 **질문 과목명(`questionPost.subject`)** 기반 동적 값으로 변경(없으면 "질문 상세"). subtitle은 작성자 전공으로 유지.
   - 이슈에 언급된 대로 피그마상 별도 헤더가 아직 확정되지 않아, 최소한 질문별로 의미 있는 과목명이 노출되도록 했습니다.
2. 최상위 배경을 `bg-gray-100`으로, 본문 컨테이너에만 `bg-white`를 적용 → PC에서 컨테이너 영역만 흰색, 바깥은 회색(다른 페이지와 동일한 모바일-퍼스트 PC 레이아웃 패턴).
3. 로딩 스켈레톤 화면도 동일하게 적용.

### 📂 변경 파일
- `src/app/board/question/detail/[id]/page.tsx`

### 🔍 검증
- `npm run build` → `✓ Compiled successfully` (에러 0)

> 커밋: `질문상세 헤더 수정 : fix : ...`
